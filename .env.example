# Figma personal access token used by the figma MCP server (figma-developer-mcp).
# Generate one at: https://www.figma.com/settings → Security → Personal access tokens.
# Copy this file to `.env` and replace the placeholder with your real token.
# `.env` is gitignored — never commit it.
FIGMA_API_KEY=figd_your_token_here
